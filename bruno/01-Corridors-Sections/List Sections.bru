meta {
  name: List Sections
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/railways/sections/
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8000
}

assert {
  res.status: eq 200
  res.body: isDefined
}
